def pegasos_kernel_svm(
    data: np.ndarray,
    labels: np.ndarray,
    kernel="linear",
    lambda_val=0.01,
    iterations=100,
    sigma=1.0,
) -> (list, float):
    # Your code here
    return alphas, b
